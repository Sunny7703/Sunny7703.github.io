#include <iostream>
#include <cstdio>
using namespace std;

int main()
{
    double ax, ay, bx, by, cx, cy, dx, dy;
    while (scanf("%lf %lf %lf %lf %lf %lf %lf %lf",
                 &ax, &ay, &bx, &by, &cx, &cy, &dx, &dy) == 8)
    {



    }
    return 0;
}
