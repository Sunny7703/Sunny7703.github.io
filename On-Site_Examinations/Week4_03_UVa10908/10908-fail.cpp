#include <iostream>
#include <string>
using namespace std;

int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        int M, N, Q;
        cin >> M >> N >> Q;

        string g[105];
        for (int i = 0; i < M; i++)
            cin >> g[i];

        while (Q--)
        {
            int r, c;
            cin >> r >> c;



        }
    }
    return 0;
}
