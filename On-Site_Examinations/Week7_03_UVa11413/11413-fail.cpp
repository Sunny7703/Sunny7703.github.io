#include <iostream>
#include <vector>
using namespace std;

int n, m;
vector<int> c;

int main()
{
    while (cin >> n >> m)
    {
        c.assign(n, 0);
        for (int i = 0; i < n; i++) cin >> c[i];



    }
    return 0;
}
