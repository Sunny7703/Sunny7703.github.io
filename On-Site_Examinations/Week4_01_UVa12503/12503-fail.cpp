#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;

        vector<int> move(n + 1); // move[i] = +1 or -1 for instruction i
        int pos = 0;

        for (int i = 1; i <= n; i++)
        {
            string cmd;
            cin >> cmd;

        }

    }
    return 0;
}
