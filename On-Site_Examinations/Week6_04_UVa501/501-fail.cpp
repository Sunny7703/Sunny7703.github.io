#include <cstdio>
#include <queue>
#include <vector>
using namespace std;

int main()
{
    int T;
    scanf("%d", &T);
    while (T--)
    {
        int M, N;
        scanf("%d %d", &M, &N);

        vector<int> a(M), u(N);
        for (int i = 0; i < M; i++) scanf("%d", &a[i]);
        for (int i = 0; i < N; i++) scanf("%d", &u[i]);



    }
    return 0;
}
