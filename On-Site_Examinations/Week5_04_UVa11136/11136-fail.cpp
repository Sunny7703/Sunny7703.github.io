#include <iostream>
#include <set>
using namespace std;

int main()
{
    int n;
    while (scanf("%d", &n) == 1 && n != 0)
    {
        multiset<long long> s;
        long long sum = 0;

        for (int day = 0; day < n; day++)
        {
            int k;
            scanf("%d", &k);



        }
    }
    return 0;
}
